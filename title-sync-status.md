# 新增功能：标签页标题同步状态提示

## 目标

新增一个纯展示层功能：在浏览器标签页标题开头显示当前远程同步状态，让用户在 S1 页面切到后台时，也能通过标签页标题看到同步进度或结果。

本功能只负责显示状态：

- 不新增同步触发逻辑
- 不修改远程同步流程
- 不修改冲突处理逻辑
- 不改变现有导航栏同步状态提示器的行为
- 只复用现有同步状态来源

实现方式参考现有“自定义标题后缀”功能。区别是：

- 自定义标题后缀：追加到标题末尾
- 本功能：追加到标题开头，作为同步状态前缀

最终标题结构应为：

[同步状态前缀] 原始标题 自定义标题后缀

如果没有同步状态前缀，则标题应恢复为：

原始标题 自定义标题后缀

注意：不要重复叠加前缀。每次更新标题前，应先移除本功能上一次添加的同步状态前缀，再重新组合标题。

---

## 状态显示规则

标题前缀根据同步状态显示不同文字：

### 同步中

显示动画前缀：

[同步中.]
[同步中..]
[同步中...]

每 0.5 秒切换一次，循环播放。

### 同步成功

显示：

[同步成功]

持续 2 分钟后自动消失。

### 同步失败

显示：

[同步失败]

持续 5 分钟后自动消失。

### 冲突

显示：

[冲突]

持续 10 分钟后自动消失。

### 待命 / 待处理 / 无同步状态

不显示任何前缀，标题恢复原样。

---

## 状态切换规则

同步中状态出现时，应立即显示 [同步中.] 动画，并覆盖之前的成功、失败、冲突提示。

同步结束后，根据最终结果显示：

- 成功：[同步成功]
- 失败：[同步失败]
- 冲突：[冲突]

结果状态在对应时间后自动消失。

如果在结果状态尚未过期时，又开始了新的同步，则立即切换回“同步中”动画。

---

## 多标签页协调规则

这是本功能最关键的部分。

用户可能同时打开多个 S1 标签页。为了避免多个标签页标题同时显示状态，本功能必须在同设备的多个 S1 标签页之间进行协调。

### 核心行为

1. 只要有任意一个 S1 标签页处于前台，所有 S1 标签页都不显示标题同步状态。
2. 只有当所有 S1 标签页都处于后台时，才允许一个标签页显示同步状态。
3. 当所有 S1 标签页都在后台时，只允许“负责人标签页”显示同步状态。
4. 如果当前负责人标签页被关闭、崩溃或失效，剩余标签页应自动重新选出新的负责人标签页。
5. 非负责人标签页始终保持原始标题，不显示同步状态前缀。

### 前台判断

前台判断以 document.visibilityState === 'visible' 为准。

只要任意存活的 S1 标签页 visibilityState 为 visible，所有标签页都不显示标题同步状态。

### 负责人标签页定义

“负责人标签页”定义为：

当前仍然存活的 S1 标签页中，createdAt 最早的那个标签页。

不要尝试读取浏览器标签栏的真实排列顺序，因为用户脚本通常无法可靠获得浏览器标签页的左右顺序。

### 推荐实现方式

推荐使用：

- localStorage
- storage event
- 每个标签页的 heartbeat
- document.visibilitychange
- beforeunload / pagehide

每个 S1 标签页启动时生成一个唯一 tabId，并记录：

{
  tabId,
  createdAt,
  lastSeen,
  isVisible
}

每个标签页定期刷新自己的 lastSeen。

如果某个标签页的 lastSeen 超过 2 分钟没有更新，则认为该标签页已经关闭、崩溃或失效，应从协调判断中忽略。

---

## 设置面板

在设置面板的：

设置同步 → 状态显示

区块里，在“显示导航栏同步状态”开关后面，新增一个开关。

### 开关信息

标签文字：

显示标签页标题同步状态

设置项名称：

syncShowTitleSyncStatus

存放位置：

s1p_settings.syncShowTitleSyncStatus

默认值：

true

说明文字：

开启后，仅当所有 S1 标签页都不在前台时，第一个标签页标题会在同步发生时显示状态提示（同步中/成功/失败/冲突）。

### 行为要求

- 样式完全复用“显示导航栏同步状态”开关
- 当远程同步关闭时，该开关置灰禁用
- 禁用行为与“显示导航栏同步状态”开关保持一致
- 该设置项应随设置同步
- 用户关闭该开关后，所有标签页应立即停止显示标题同步状态
- 用户关闭该开关后，不应继续保留标题动画 timer

---

## 标题兼容性要求

本功能必须与现有“自定义标题后缀”功能兼容。

标题组合顺序为：

[同步状态前缀] 原始标题 自定义标题后缀

例如：

[同步中...] Stage1st - 某帖子 - S1 Plus

如果同步状态消失，应恢复为：

Stage1st - 某帖子 - S1 Plus

要求：

- 不覆盖现有自定义标题后缀
- 不影响页面原本的标题变化
- 不重复叠加同步状态前缀
- 不在多个地方无序直接写 document.title
- 如果当前代码已有统一标题刷新函数，应优先复用或扩展它
- 如果没有统一标题刷新函数，应尽量新增一个小型标题组合函数，统一处理标题前缀和后缀

---

## 资源消耗要求

只在真正需要显示同步状态时才启动标题动画和必要的协调检查。

以下情况必须停止动画 timer：

- 当前标签页不再是负责人标签页
- 任意 S1 标签页切到前台
- 用户关闭 syncShowTitleSyncStatus
- 远程同步关闭
- 状态提示过期
- 页面 pagehide / beforeunload

非负责人标签页不应运行标题动画 timer。

如果不需要显示状态，不要持续进行无意义的高频轮询。

---

## 各场景预期行为

### 单标签页，在前台

不显示任何同步状态前缀。

原因：用户正在看页面，已有页面内提示，不需要标题提示。

### 单标签页，切到后台

如果同步正在进行，标题显示“同步中”动画。

同步完成后，标题显示成功、失败或冲突结果，并在对应时间后自动消失。

### 多个标签页，全部在后台

只允许负责人标签页显示同步状态。

其他标签页保持原始标题。

### 多个标签页，其中一个切到前台

所有标签页立即停止显示同步状态。

### 当前负责人标签页被关闭

剩余标签页重新选举负责人标签页。

新的负责人标签页继续承担标题状态显示。

### 某个标签页异常崩溃

如果该标签页超过 2 分钟没有刷新 heartbeat，则认为它已经失效，并从标签页列表中忽略。

不能因为一个崩溃标签页残留记录，导致系统误判“永远有一个标签页在前台”。

### 用户关闭开关

所有标签页立即停止显示标题同步状态。

标题恢复原样。

后台不再保留标题动画 timer。

### 远程同步关闭

该功能不显示任何状态。

设置面板中的“显示标签页标题同步状态”开关置灰禁用。

---

## 验收标准

完成后需要满足：

1. 前台页面永远不显示标题同步状态。
2. 只要任意 S1 标签页在前台，所有标签页都不显示标题同步状态。
3. 所有 S1 标签页都在后台时，只有一个标签页显示同步状态。
4. 同步中动画每 0.5 秒切换一次。
5. 同步成功 2 分钟后自动消失。
6. 同步失败 5 分钟后自动消失。
7. 冲突状态 10 分钟后自动消失。
8. 关闭负责人标签页后，其他标签页可以自动接管。
9. 崩溃或异常关闭的标签页超过 2 分钟后会被忽略。
10. 与自定义标题后缀功能兼容。
11. 不重复叠加标题前缀。
12. 关闭开关后，所有标题恢复原样，并停止相关 timer。
13. 远程同步关闭时，该开关置灰禁用。
14. 不修改现有同步核心逻辑。