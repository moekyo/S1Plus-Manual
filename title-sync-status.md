# 标签页标题同步状态提示

## 功能目标

在浏览器标签页的标题上，以文字形式显示当前同步的状态，让用户在标签页切到后台时也能看到同步进度。

参考现有导航栏同步状态提示器的各个状态，在页面标题上以前缀的形式体现。实现方式参考"自定义标题后缀"功能——它往标题末尾追加文字，我们这个功能则往标题开头追加同步状态前缀。

## 外观表现

标题前缀根据不同同步状态显示不同文字：

- **同步中**：`[同步中.]` → `[同步中..]` → `[同步中...]` 循环切换，每 0.5 秒变一次，模拟动画效果
- **同步成功**：`[同步成功]`，持续 2 分钟后自动消失
- **同步失败**：`[同步失败]`，持续 5 分钟后自动消失
- **冲突**：`[冲突]`，持续 10 分钟后自动消失
- **待命、待处理**：不显示任何前缀，标题恢复原样

## 多标签页行为

这是本功能最关键的规则。因为一个用户可能同时打开多个 S1 标签页，如果每个都显示同步状态，标题会乱成一团。所以需要跨标签页协调：

1. **只要有一个 S1 标签页在前台（用户正在看），所有标签页都不显示同步状态**——用户盯着屏幕呢，不需要在标题上提示
2. **所有标签页都在后台时，只让排在最前面的那一个标签页显示同步状态**——有一个提示就够了
3. **如果正在显示的标签页被关闭了，下一个标签页自动接管**，继续显示
4. **同设备上能正确知道一共有几个 S1 标签页，各自是在前台还是后台**

达到的效果：用户切到别的应用去了，切回来之前瞄一眼标签栏，就能从一个标签上看到同步是成功了还是失败了。

## 设置面板

在设置面板的"设置同步"→"状态显示"区块里，跟在"显示导航栏同步状态"开关后面，新增一个开关：

- 标签文字：`显示标签页标题同步状态`
- 样式：完全复用"显示导航栏同步状态"那个开关的样式
- 默认值：开启
- 当远程同步关闭时，这个开关置灰禁用（跟上面那个开关行为一致）
- 说明文字：`开启后，仅当所有 S1 标签页都不在前台时，第一个标签页标题会在同步发生时显示状态提示（同步中/成功/失败/冲突）。`
- 这个开关的设置项名称叫 `syncShowTitleSyncStatus`，放在 `s1p_settings` 对象里，随设置同步

## 各种情况处理

### 单标签页，在前台
不显示任何同步前缀——用户正看着页面，不需要在标题上提示。

### 单标签页，切到后台
如果此时有同步在进行，标题上就开始走"同步中"的动画。同步完成后自动切换为"同步成功"（或失败/冲突），持续若干分钟后自动消失。

### 多个标签页，都在后台
只有排在最前面的那个标签显示同步状态。其他标签页标题保持原样。

### 多个标签页，有一个切到前台
所有标签页立即停止显示同步状态——因为有一个已经在前台了。

### 正在显示的标签页被关掉
剩余的标签页立刻重新决策：新的"排最前面"的标签页接管显示。

### 用户关掉了这个开关
所有标签页立刻停止显示同步状态，并且后台不再有任何轮询开销。

### 标签页崩溃（非正常关闭）
如果某个标签页因为崩溃没有正常清理，它留在记录里超过一段时间（比如 2 分钟）没人理，就自动被忽略——不会导致"永远有一个标签在前台"的假象。

### 标题被其他功能修改
这个功能追加前缀的方式，要和现有的"自定义标题后缀"功能兼容——标题后缀照常显示，前缀加在最前面。两者互不覆盖。

### 动画资源消耗
只在真正需要显示同步状态时才启动标题动画和轮询检查。不需要显示时（比如标签在前台、或者不是排最前面的标签），完全没有额外的定时器在跑。

### 同步提示的时效
"同步成功"之类的状态不会永远挂在标题上，会在一段时间后自动消失（成功 2 分钟、失败 5 分钟、冲突 10 分钟）。这段时间足够用户切回来瞄一眼了。
