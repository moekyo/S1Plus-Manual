{
  "name": "s1plus-userscript",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-userscript.mjs",
    "check:bundle": "node scripts/check-userscript-build.mjs",
    "verify:bundle": "npm run build && npm run check:bundle"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "esbuild": "0.28.0"
  }
}
