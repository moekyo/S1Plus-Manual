# S1 Plus 更新说明

## 本次更新亮点

### 📱 窄屏兼容补丁
- 修复了 **NUX 窄屏模式** 下，启用 S1 Plus 后帖子头部（头像/用户名/时间/工具栏）可能错位的问题。
- 修复了列表页三点按钮的二次确认菜单在窄屏下“固定向左弹出”导致超出可视区域的问题；现在会按可视空间自动改为向右弹出。
- 修复了帖子页“当前阅读位置”标识在窄屏下可能遮挡工具条或贴近楼层号的问题：窄屏自动改为紧凑图标，并优化了与楼层号的间距。

### 🔒 更安全可靠
- 全面加固了脚本安全性，修复了潜在的 XSS 风险和原型链污染问题
- 优化了 HTML 清洗器，仅允许安全的链接协议
- 新增了正则表达式 ReDoS 防护，防止恶意正则表达式导致的性能问题

### ☁️ 同步功能大升级
- **更稳定**：远程请求增加了超时和重试机制，网络不稳定时也能顺利同步
- **更智能**：新增跨标签页锁机制，防止多标签页同时同步导致的冲突
- **更省心**：阅读进度冲突自动合并，无需手动干预
- **更强大**：支持 Gist 大文件（>1MB）自动拉取完整内容
- **新增同步诊断**：三连击版本号可查看同步诊断信息，方便排查问题

### 🎨 界面交互优化
- **工具栏图标化**：帖子楼层操作按钮（只看该用户、收藏、屏蔽等）全部升级为 SVG 图标，界面更清爽
- **统一工具提示**：所有图标按钮都有了统一风格的悬浮提示
- **动画更柔和**：阅读进度更新改为外发光效果，减少视觉干扰
- **工具栏按钮延迟**：操作按钮的悬浮提示增加 1 秒延迟，避免鼠标划过时的干扰
- **图标尺寸统一**：正序/倒序浏览图标大小调整，视觉更协调

### ✨ 新功能与增强
- **GitHub Token 过期提醒**：可设置 Token 过期时间，过期前 3 天提醒更新
- **Token 显示/隐藏**：同步设置中新增 Token 显示/隐藏按钮，方便校验
- **用户标记颜色自定义**：为用户标记添加 6 种预设颜色可选
- **历史标记快速选择**：编辑标记时可快速选择历史使用过的标记
- **楼层屏蔽功能**：可屏蔽单个楼层，无需屏蔽整个用户
- **屏蔽用户备注**：屏蔽用户时可添加备注信息
- **手动用户名屏蔽**：设置面板支持手动输入用户名（支持 `@用户名`），自动识别 UID 并可同时填写备注
- **系统屏蔽楼层隐藏**：可自动隐藏被系统屏蔽的楼层
- **手动清理阅读记录**：支持手动选择清理范围，可查看阅读记录详情
- **收藏同步可配置**：可选择同步完整正文或仅预览（默认预览以减少同步体积）
- **用户详情页屏蔽按钮**：在用户主页顶部新增"屏蔽该用户"按钮，支持填写备注、同步至论坛黑名单；已屏蔽时点击可快速解除屏蔽

### 🐛 大量问题修复
- 修复了列表页偶发底部横向滚动条和右侧空白区的问题（右开菜单样式仅在悬停态生效，并在鼠标离开后清理状态）
- 修复了设置跨标签页刷新不稳定的问题
- 修复了导入数据后阅读进度跟踪中断的问题
- 修复了设置面板事件监听重复绑定的问题
- 修复了仅开启楼层屏蔽时按钮不显示的问题
- 修复了论坛黑名单同步状态不一致的问题
- 修复了同帖多标签页旧标签覆盖新进度的问题
- 修复了 Token 过期弹窗与每日同步冲突的问题
- 修复了关闭用户屏蔽时恢复不完整的问题
- 修复了导入旧格式数据时时间戳错误的问题
- 修复了导航链接识别范围过宽的问题
- 修复了关闭阅读进度后观察器未停止的问题
- 修复了保存同步设置后触发条件偏宽的问题
- 修复了取消屏蔽用户时本地/论坛状态分裂的问题
- 修复了关闭帖子屏蔽时表头占位列残留的问题
- 修复了悬浮菜单监听器累积的问题
- 修复了导入数据后页面屏蔽状态不一致的问题
- 修复了屏蔽/取消屏蔽用户后提醒区未刷新的问题
- 修复了评分列表中“全部评分都来自已屏蔽用户”时仅隐藏条目但表头残留的问题
- 修复了禁用 NUX 时设置面板样式异常的问题
- 修复了手动清理模式下仍自动清理的问题
- 修复了默认颜色勾选图标不可见的问题
- 修复了清理模式切换动画异常的问题

### ⚡ 性能优化
- 核心数据读取增加缓存，减少重复读取开销
- 高频集合匹配优化，查找复杂度从 O(n) 降至 O(1)
- 阅读进度写入批量落盘，减少频繁写入
- MutationObserver 节点级增量处理，避免全量重渲染
- 标题规则正则编译缓存，规则未变时跳过重新编译

---

## 更新建议
本次更新包含大量安全性和稳定性改进，建议所有用户尽快更新。更新后如遇到任何问题，可通过设置面板中的同步诊断功能（三连击版本号）查看详细信息。
