# S1 Plus 弹窗全局审计记录

审计日期：2026-04-30

## 范围

本次审计覆盖 `S1Plus.js` 中的主要弹窗和弹层家族：

- 设置面板：`.s1p-modal`
- 通用确认弹窗：`.s1p-confirm-modal`
- 高级确认弹窗：`createAdvancedConfirmationModal(...)`
- 输入确认弹窗：`createInputModal(...)`
- 手动同步选择弹窗：`.s1p-sync-modal`
- Token 有效期配置弹窗：`.s1p-token-config-modal`
- 阅读记录详情弹窗：`.s1p-reading-progress-modal`
- 图片查看器：`.s1p-image-viewer`
- 日期选择器：`.s1p-date-picker`

本轮已实际修复的范围仅限手动同步选择弹窗的小屏显示问题。其他内容是审计记录和后续修复建议。

## 总结

当前脚本并没有对所有弹窗做统一的小屏和可访问性优化。设置面板、图片查看器已经有较完整的响应式基础；手动同步弹窗已经补齐视口高度限制、正文滚动和底部按钮可见性。通用确认弹窗和高级确认弹窗仍是主要风险来源，因为它们被多个功能复用，却还没有统一的高度约束、滚动策略、焦点管理和 dialog 语义。

问题统计：

- 高优先级：3 项
- 中优先级：3 项
- 低优先级：1 项

## 高优先级问题

### 1. 通用确认弹窗仍可能在小屏下被截断

位置：

- `S1Plus.js:4325`，`.s1p-confirm-content`
- `S1Plus.js:25314`，`createConfirmationModal(...)`
- `S1Plus.js:25430`，`createInputModal(...)`
- `S1Plus.js:32338`，`createAdvancedConfirmationModal(...)`

现状：

`.s1p-confirm-content` 使用 `width: 480px` 和 `max-width: 90%`，但没有全局 `max-height`、正文滚动、底部按钮区保底可见等规则。手动同步弹窗已经单独通过 `.s1p-sync-modal` 修复，但其它确认弹窗没有继承这些保护。

影响：

在小屏、横屏、浏览器缩放、系统字体放大或正文较长时，按钮可能落到视口外，用户无法安全完成或取消操作。

建议：

将手动同步弹窗已验证的结构抽象到通用确认弹窗层：

- `.s1p-confirm-content` 支持 `max-height: calc(100dvh - safe padding)`
- `.s1p-confirm-content` 使用纵向 flex
- `.s1p-confirm-body` 作为可滚动区域
- `.s1p-confirm-footer` 固定留在底部并允许按钮换行
- `.s1p-sync-modal` 只保留同步表格和按钮宽度等专属细节

建议使用技能：`/adapt`、`/harden`

### 2. 多数弹窗缺少标准 dialog 语义

位置：

- `S1Plus.js:26712`，设置面板 `.s1p-modal`
- `S1Plus.js:25324`，通用确认弹窗
- `S1Plus.js:32359`，高级确认弹窗
- `S1Plus.js:26298`，Token 配置弹窗
- `S1Plus.js:36385`，阅读记录详情弹窗

现状：

图片查看器的面板已经有 `role="dialog"`、`aria-modal="true"` 和 `aria-label`。其它弹窗大多只是视觉上的 modal，没有统一设置 dialog 语义、标题关联或描述关联。

影响：

屏幕阅读器用户可能无法明确知道当前进入了一个弹窗，也可能无法关联弹窗标题和正文。对辅助技术来说，这些弹窗更像普通页面内容。

建议：

建立统一的弹窗初始化 helper，为弹窗内容容器设置：

- `role="dialog"`
- `aria-modal="true"`
- `aria-labelledby`
- `aria-describedby`，当正文存在时
- 稳定的标题和正文 id

建议使用技能：`/harden`

### 3. Esc、初始焦点和关闭后焦点回收不统一

位置：

- `S1Plus.js:660`，已有 `bindEscCloseForStyledModal(...)`
- `S1Plus.js:25314`，`createConfirmationModal(...)`
- `S1Plus.js:32338`，`createAdvancedConfirmationModal(...)`
- `S1Plus.js:28130`，`openManualUserBlockModal(...)`

现状：

设置面板、Token 配置、阅读进度详情和图片查看器有不同程度的 Esc 处理。通用确认弹窗和高级确认弹窗没有统一绑定 Esc，也没有统一的打开后聚焦策略和关闭后恢复焦点策略。

影响：

键盘用户体验不一致；某些弹窗只能点击按钮或遮罩关闭。关闭后焦点可能丢失到页面正文，影响连续操作。

建议：

新增或扩展统一 modal lifecycle helper：

- 打开前记录触发元素
- 打开后聚焦首个合适按钮或输入框
- Esc 关闭时遵循取消动作
- 关闭后恢复原触发元素焦点
- 必要时加入焦点环绕，避免 Tab 跳到遮罩背后的页面

建议使用技能：`/harden`

## 中优先级问题

### 4. Token 日期选择器可能贴边或超出视口

位置：

- `S1Plus.js:3173`，`.s1p-date-picker`
- `S1Plus.js:26209`，日期选择器 `updatePosition()`

现状：

日期选择器固定宽度 `280px`，总是优先显示在输入框下方，并从输入框左侧开始定位。当前代码注释中也保留了“边界检查可选优化”的提示，但实际没有处理。

影响：

在小屏、输入框靠右、弹窗接近底部或页面滚动状态复杂时，日期选择器可能溢出视口或被用户误认为消失。

建议：

为 `updatePosition()` 增加边界计算：

- 右侧溢出时向左收回
- 下方空间不足时显示在上方
- 小屏下宽度使用 `min(280px, calc(100vw - 16px))`
- 使用 `100dvh` 或 `visualViewport.height` 处理移动浏览器可视高度

建议使用技能：`/adapt`

### 5. 部分触控目标偏小

位置：

- `S1Plus.js:2628`，行内确认按钮 `.s1p-confirm-action-btn`
- `S1Plus.js:3709`，设置面板关闭按钮
- `S1Plus.js:5686`，图片查看器工具栏图标按钮
- `S1Plus.js:5794`，图片查看器左右切换按钮

现状：

多个按钮尺寸在 28px 到 38px 之间。桌面鼠标环境下可用，但触屏上低于常见 44px 建议值。

影响：

小屏触控误触概率更高，尤其是关闭、确认、取消、图片左右切换这类高影响操作。

建议：

只在窄屏或触控媒体查询下放大命中区域：

- `@media (pointer: coarse)` 中提升最小命中区域到 44px
- 保持图标视觉尺寸不变，通过 padding 或伪元素扩大点击区域
- 不强行改变桌面密度

建议使用技能：`/adapt`

### 6. 部分小屏高度仍使用 `100vh`，缺少 `100dvh` 兜底

位置：

- `S1Plus.js:5216`，设置面板窄屏高度
- `S1Plus.js:5962`，图片查看器窄屏高度

现状：

手动同步弹窗已补充 `100dvh`。设置面板和图片查看器仍主要依赖 `100vh`。

影响：

移动浏览器地址栏显示/隐藏时，可视高度可能与 `100vh` 不一致，造成底部区域被遮住或出现多余空白。

建议：

在现有 `100vh` 后追加 `100dvh` 覆盖，并确认不影响旧浏览器：

```css
max-height: calc(100vh - 16px);
max-height: calc(100dvh - 16px);
```

建议使用技能：`/adapt`

## 低优先级问题

### 7. 高级确认弹窗按钮样式层级不够清晰

位置：

- `S1Plus.js:32392`，高级确认弹窗 footer
- `S1Plus.js:36991`，Token 过期提醒三个操作按钮

现状：

高级确认弹窗允许传入多个按钮，但视觉层级主要依赖传入的 class。部分调用会传入普通 `s1p-btn` 或多个近似按钮，可能导致主次关系不够明显。

影响：

在有三个操作时，用户需要多花一点时间判断哪个是主操作。当前不是阻塞问题，但可以提升确认效率。

建议：

约定高级确认弹窗的按钮策略：

- 只有一个主按钮使用 `s1p-confirm`
- 次要动作使用普通按钮
- 取消或破坏性退出使用 `s1p-cancel`
- 多按钮在小屏下允许换行并保持主按钮位置稳定

建议使用技能：`/clarify`、`/polish`

## 正向发现

- 设置面板已有较完整的窄屏适配，包括整体宽度、tabs 横向滚动、设置项换行和部分控件横向滚动。
- 图片查看器已经具备较好的弹窗语义、`aria-live` 状态提示、减弱动效处理和响应式工具栏。
- 样式大多使用 `var(--s1p-...)` 主题变量，深色模式维护成本相对可控。
- 手动同步弹窗已经补齐本次最直接的问题：小屏下正文可滚动、按钮区可见、表格列宽和长时间文本可换行。

## 推荐修复顺序

1. **立即处理**：把手动同步弹窗的视口高度、正文滚动、按钮区可见策略推广到通用确认弹窗。
2. **短期处理**：为所有 modal 工厂补统一 dialog 语义、Esc 行为、初始焦点和关闭后焦点恢复。
3. **中期处理**：修复 Token 日期选择器的视口边界定位。
4. **中期处理**：在触控环境下扩大关键按钮命中区域。
5. **长期处理**：整理高级确认弹窗的按钮层级规范，减少多按钮场景下的判断成本。

## 建议的实现切分

### 切分 1：通用确认弹窗响应式基线

目标：

- 修改 `.s1p-confirm-modal` / `.s1p-confirm-content` / `.s1p-confirm-body` / `.s1p-confirm-footer`
- 保证小屏下正文滚动，按钮始终可见
- 保持 `.s1p-sync-modal` 的表格专属优化

验证：

- 手动同步选择弹窗
- 欢迎更新弹窗
- NUX 推荐弹窗
- Token 过期提醒弹窗
- 删除确认弹窗
- 输入备注弹窗

### 切分 2：弹窗语义与焦点生命周期

目标：

- 给通用确认、设置、Token 配置、阅读进度详情补 dialog 语义
- 统一 Esc、初始焦点、关闭后焦点恢复
- 尽量复用 `bindEscCloseForStyledModal(...)`

验证：

- 键盘只用 Tab / Shift+Tab / Enter / Esc 完成常见操作
- 打开和关闭后焦点位置合理

### 切分 3：日期选择器和触控命中区

目标：

- 日期选择器自动避让视口边界
- 触控环境下关键按钮命中区域不小于 44px

验证：

- 320px 宽度
- 纵向小高度
- 页面滚动后打开 Token 日期选择器
- 图片查看器触控按钮
